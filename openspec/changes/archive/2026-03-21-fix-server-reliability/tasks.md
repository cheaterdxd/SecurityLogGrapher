## 1. Security & Reliability

- [x] 1.1 Add directory traversal validation to `stream_evtx` in `server.py`
- [x] 1.2 Add `try-except` cleanup logic in `upload_evtx` in `server.py`

## 2. User Experience

- [x] 2.1 Replace `time.sleep` with a polling mechanism in `main.py` `_open_browser` function
